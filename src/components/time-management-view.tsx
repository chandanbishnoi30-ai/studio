'use client';

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function TimeManagementView() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Time Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is where you can manage your time and tasks.</p>
      </CardContent>
    </Card>
  );
}
